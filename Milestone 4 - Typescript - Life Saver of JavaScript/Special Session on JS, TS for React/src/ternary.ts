const age = 20;
const result = age >= 18 ? 'Adult' : 'Child';
console.log(result);

if (age >= 18) {
    console.log('Biyer Boyos Hoice');
} else {
    console.log('Biyer boyos hoy nai');
}

let biyerBoyos = age >= 18 ? console.log('Biyer Boyos Hoice') : console.log('Biyer boyos hoy nai');