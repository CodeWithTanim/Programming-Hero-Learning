let location = 'BBan';
let age = 18;
let boyos = 18;

// Variable name should be something meaningful. It will indicate what it contains.
let togudutogudutogudu = 97;
let avc = 45;
let info = 'mon majar'
let brb = 'Jasmin'

// Do not use shorthand.
// Its oky if the name is longer

// Can not use a keyword (reserved word)
const let = 26
// console.log(let)
// const false = 'False'

// has to be in one word

// const her phone number = 5165464654;
const her_phone_number = 5165464654; // Snake case
const herPhoneNumber = 5165464654; // camel case
const HerphoneNumber = 5165464654; // pascal case
// Name cannot start with a number. but number could be used anywhere other than the first letter.

// const 3Person = 3;
const person3 = 3;
const p3rson = 6;

// other that letter name can start with either _ , $. Cannot use any other soecial character * & # @
// case sensitive

let address = 'katabon';
let Address = 'bandarban';
let AddRess = 'shalbon';