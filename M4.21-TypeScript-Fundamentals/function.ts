// function add(num1, num2) {
//     const total = num1 + num2;
//     return total;
// };

/**
 * parameters
 * return
 * no return
 * 
*/

// function add(num1:number, num2:number): string {
// function add(num1:number, num2:number): boolean {
function add(num1:number, num2:number): number{
    const total = num1 + num2;
    return total;
};

add(5,10)
// add(5,'10')

let result = add(5,56);     // By default number + number = number
// let result:string = add(5,56);
// let result:boolean = add(5,56);







// function calculate():void {     // when no return
function calculate(numbers: number[], tax: number, accountant?: string):boolean {     // when no return
    return false;
};

calculate([23, 45, 66], 0.2, 'John');
calculate([23, 45, 66], 0.2);



function doubledOrHalf (num: number, isDoubled: boolean): boolean {
    return false;
};

